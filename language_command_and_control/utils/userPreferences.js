export function loadUserPreferences(user) {
    // Placeholder function to load user preferences (this should connect to a backend or local storage)
    return new Promise(resolve => {
        const preferences = {
            selectedPlanet: 'Spanish'
        };
        resolve(preferences);
    });
}
